// 4. Colecciones (Arrays) y Bucles

var tareas = ["Comprar leche", "Llamar a mamá", "Estudiar Swift"]

// Añadimos un nuevo elemento al array
tareas.append("Ir al gimnasio")

// El bucle lee cada tarea una por una
for tarea in tareas {
    print("Pendiente: \(tarea)")
}
